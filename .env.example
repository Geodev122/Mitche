# Environment Variables for Mitché Platform

# Port configuration (optional - defaults to 4173)
PORT=4173

# Build environment
NODE_ENV=production

# Optional: Custom domain for deployment
# CUSTOM_DOMAIN=your-domain.com

# Note: This application doesn't require API keys for basic functionality
# All features work offline-first with localStorage