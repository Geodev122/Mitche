{
  "name": "mitche-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
  "@google-cloud/secret-manager": "^6.1.0",
  "firebase-admin": "^11.11.0",
  "firebase-functions": "^5.1.1",
    "mitche-hope-bearer-platform": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
